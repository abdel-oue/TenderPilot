// TODO: it('routes a scanned PDF to the OCR path')
// TODO: it('routes a text-layer PDF to the pdf path')
// TODO: it('skips parsing when the content hash is already cached')
// TODO: it('keeps the page number on every chunk it produces')
