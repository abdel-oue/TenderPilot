// Vitest. One describe per module, one it per BEHAVIOUR.
//
// TODO: it('flags an unmet eliminatory requirement as a blocker')
// TODO: it('returns no-go when a blocker exists even with a passing score')
// TODO: it('reads the elimination threshold from the tender rubric, not a constant')
// TODO: it('returns go when every eliminatory requirement is covered')
//
// LLM mocked at the lib/llm.ts boundary. Never hit the real endpoint.
