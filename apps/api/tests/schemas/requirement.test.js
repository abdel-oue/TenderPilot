// Every zod schema gets a MALFORMED payload test, not only a valid one.
//
// TODO: it('rejects a requirement with no source page')
// TODO: it('rejects an unknown category')
// TODO: it('accepts a well-formed requirement')
