// All tender business logic. No SQL (delegate to db/tender.queries.js).
//
// TODO: list(filters)
// TODO: getById(id) - throws NOT_FOUND so the route can map the status code
// TODO: create(input) - dedupe on the AO reference
// TODO: remove(id)
