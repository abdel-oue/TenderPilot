// Web-only view types. Anything shared with the api belongs in packages/shared.
// A type duplicated between web and api is a bug.
// TODO: table column defs, filter state, nothing that crosses the wire
