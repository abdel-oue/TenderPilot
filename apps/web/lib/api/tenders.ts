// Fetch wrappers for tenders. ALL frontend fetching lives in lib/api/.
// TODO: fetchTenders(filters), fetchTender(id), createTender(body)
// TODO: parse every response against the shared zod schema before returning
// TODO: throw the api's { error, code } shape so hooks can surface the message
