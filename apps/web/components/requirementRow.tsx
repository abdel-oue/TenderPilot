// One requirement, with its provenance.
// TODO: export default function RequirementRow() - PascalCase inside, camelCase file
// TODO: props interface above the component
// TODO: shows text, category, eliminatory flag, and "CPS art. 7.3, p. 4"
// TODO: data-testid on the row - E2E selects by testid, never by class or copy
// No business logic in here. Extract to a hook or a util.
