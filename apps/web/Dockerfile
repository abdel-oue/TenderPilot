# Multi-stage: deps -> build -> runner.
# TODO: FROM node:20-alpine pinned. No :latest.
# TODO: build with next build (standalone output)
# TODO: runner carries the standalone bundle only - no devDependencies, no .ts source
# TODO: non-root user in the final stage
# TODO: no secrets baked in
