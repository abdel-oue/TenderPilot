// Playwright. One spec per user-visible step. Independent, any order, alone.
//
// TODO: test('uploads a dossier and sees it listed')
// TODO: test('runs the analysis and sees a verdict')
// TODO: test('sees the blockers cited with page and article on a no-go')
//
// Selectors: data-testid only. Never a CSS class, never copy text.
// No waitForTimeout - wait on a state, a response, or a testid.
// LLM stubbed at the api boundary via STUB_LLM.
